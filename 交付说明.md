# 乐龄桌面 · 交付说明

> 给老年人用的安卓桌面（Launcher）App · Kotlin · minSdk 26（Android 8.0+）· 重点适配小米 MIUI/HyperOS
> 更新日期：2026-09-07 · 版本 2.4.3 · 全部核心功能已在 Android 16 模拟器实测通过
> **交付安装包：`乐龄桌面-v2.4.3-release.apk`（正式签名，2.1MB）+ `tools/AlertTestSender/AlertTestSender.apk`（同签名警报推送测试端）**
> v2.2：**紧急求助 SOS**——呼叫页顶部红色「紧急求助·按住3秒」条（防误触），触发后自动呼叫第一位紧急联系人 + 群发带定位的求助短信；联系人可标记紧急（SOS 红标、桌面优先排列）；编辑页一键开关；未设置时给引导
> v2.1：**开放集成架构**——通用警报框架（AlertManager/AlertActivity，地震/洪水等任何来源可插拔）、外部警报推送广播接口（签名权限保护，实测第三方接入全链路）、设备状态只读查询 Provider（SIM/流量/电量）、开发者文档 docs/开发指南.md；SIM 警报迁移至新框架
> v2.3.1：**播报降噪**——移除「xx页」「正在打开xx」等无意义语音播报（保留呼叫/SOS/重启/天气等有意义播报）
> v2.4：**权限自检与授权（针对小米未知来源/权限被自动关闭）**——新增「权限自检与授权」页：全部所需权限实时状态（✓/✗），未授权项点击即弹系统授权框，被"拒绝不再询问"的引导跳应用详情页手动开启；含小米自启动开启路径指引；SOS/流量查询缺权限时自动降级（SOS 仍保证呼出）并向导/子女模式提供入口
> v2.4.1：**应用页多页化**（◀▶翻页不滚动）+ **拖拽换快捷栏**（长按应用图标拖到底部槽位更换）+ **WindowInsets 防遮挡**（状态栏/手势栏空间自适应）
> v3.0.0：**企业级重构**——功能开关（SOS/AI默认关设置开）、安全修复（心跳泄露PIN/密钥P0、PIN验旧值+锁定）、性能（冷启动后台化/异步IO/息屏省电）、正式PRD
> v2.8.1：**防沉迷**——子女模式配置每应用「每天限额/每次限额/禁用时段（如22点后禁抖音）」；三层执行（启动拦截+每分钟看门狗+DO硬管控隐藏）；需授权"使用情况访问"（设置内有入口）；阻断为老人友好大字页
> v2.7.2：**显示不全修复**——生活页改弹性布局：日历7行均分剩余高度（任意屏幕/字号不裁剪）、三天预报横排
> v2.7.1：**分析与优化轮**——标签缓存/挂断监听自停（省电）/卡片按压涟漪/网格刷新防闪烁；内存 49MB、全功能回归通过
> v2.7.0：**AI 对话页重设计**（新气泡+贴底堆叠+单条重听+渐变麦克风）+ **性能优化**（图标缓存/无变化跳过刷新）+ **兼容适配**（大字号/小屏实测通过、全页面竖屏锁定）
> v2.6.0：**AI 双协议 + 视觉打磨**——大模型客户端同时支持 OpenAI 兼容与 Anthropic Messages 格式（按地址自动识别），实测接入本地 Claude 中转（GLM 模型）真实对话成功；底部导航/卡片阴影与留白打磨
> v2.5.1：**拖拽修复**——底部快捷栏拖放不生效的根因（DRAG_STARTED 未接受）修复，实测端到端拖放成功
> v2.5.0：**小米内置应用修复**——白名单/快捷栏默认改为按系统能力意图解析（拨号/短信/相机/浏览器/时钟等，任何 ROM 通用）+ 国产常用应用自动纳入；拖拽换快捷栏增加震动与文字提示
> v2.4.5：**应用多页修复**——每个应用子页内容各不相同（此前所有子页都重复第1页内容）；列表切换/显示全部后即时刷新
> v2.4.3：**稳定性修复**——彻底解决「按钮没反应/应用反复停止」的崩溃（嵌套 RecyclerView 布局期间 notify 引发），并对所有同类调用加防御
> v2.3：**SOS 界面降噪**——联系人卡不再显示 SOS 角标；紧急联系人开关移入「子女模式 → 联系人 → 紧急联系人」多选配置（触发条保留）
> v2.0.1：**安全加固**——SIM警报页停止对外导出、debug_force 仅 debug 构建生效、release 日志脱敏（不再记录短信内容/号码）、关闭云备份（联系人不出设备）、位图缓存按堆大小自动配额
> v2.0：**底部应用快捷栏**——移除语音问答/生活/常用应用三个导航按钮和「乐龄桌面」文字，底栏改为可配置的应用快捷方式（默认电话/短信/相机，子女可换，最多3个）+ 固定一键重启；子女设置新增「底部快捷栏」配置；隐藏入口移到指示点行（连点5次+PIN）
> v1.9/1.9.1：**视觉降噪与交互回归**——联系人照片压矮、三页顶部留白、底部白色卡片导航栏（四按钮交互全量实测）；修复"添加联系人"虚线卡撑满整屏
> v1.8/1.8.1：**联系人 1 寸照片式小卡**（点照片直接呼叫，长按发短信/编辑；大小每行1/2/3可调）、**生活页**（左实时大时钟｜右当日日期+农历；天气卡居中；日历在下方，点日期看农历详情）、**应用图标大小可调**
> v1.7：**呼叫页降噪**——去掉顶部信息堆和页标题，联系人直接开始；只在异常时显示红色提醒条；时间/农历/流量/电量归位「生活」页
> v1.6：**通话中挂断条**（桌面顶部红色大按钮，TelecomManager 真挂断）+ **联系人卡视觉修复**（两列大卡/正圆头像/按钮归位）
> v1.5：**基础通信补全**——联系人卡「呼叫/短信」双操作、大字发短信页（常用短语一键填入，真实发送）、长按打开短信收件、呼出前大字确认
> v1.4：**真呼出强化**（点头像直接经运营商呼出+运行时补授权）、**整体布局重设计**（顶部速览+四按钮导航+生活页去重）
> v1.3：**生活页**（大时钟+农历 / 在线天气+语音播报 / 本月日历），三页桌面
> v1.2：**子女设置隐藏入口**（连点5次+PIN 双保险）、全程语音播报
> v1.1：**流量智能管理**（每天短信查流量，不足自动关数据，月结自动恢复）、**选择性接管**

---

## 1. 交付物

| 文件 | 说明 |
|------|------|
| `app/build/outputs/apk/debug/app-debug.apk` | 可直接安装的 APK（约 3MB） |
| `docs/需求规划文档.md` | 完整需求/技术方案/页面设计/测试报告/调研附录 |
| `docs/shots/` | 44 张验证截图 + UI 层级转储（01~44 编号对应测试用例） |
| `app/src/main/` | 全部源码（16 个 Kotlin 文件 + 布局/资源） |
| `tools/` | 测试辅助脚本（shot.sh 截图脚本、mock_llm.py 模拟大模型服务） |

## 2. 构建方法

```bash
# 需要 JDK 17+（Android Studio 自带 JBR 即可）和 Android SDK（platform 35）
set JAVA_HOME=C:\Program Files\Android\Android Studio\jbr
gradlew.bat assembleDebug
# 产物: app/build/outputs/apk/debug/app-debug.apk
adb install -r app-debug.apk
```

## 3. 真机（小米等）安装与配置流程（由子女操作，约 5 分钟）

1. **安装** APK，首次打开自动进入「为父母配置手机」向导；
2. **第 1 步 设为默认桌面**：向导里点按钮跳系统设置，选「乐龄桌面」并设为始终；
3. **第 2 步 一键授权**：电话/通讯录/麦克风（一次弹窗全授权）；
4. **第 3 步 添加联系人**：手动填或从通讯录导入（姓名+长号+短号+头像：相册照片或 8 个预设头像），保存后头像卡片即上桌面；
5. **（推荐）激活一键静默重启**：
   - 手机恢复出厂后不登录任何账号（或先退出小米账号）；
   - 电脑执行：`adb shell dpm set-device-owner com.safphere.launcher/.admin.ElderDeviceAdminReceiver`
   - 提示 Success 后，「一键重启」两步点击即可静默重启（无需电源菜单）；
   - 没有电脑的替代：子女模式 →「开启无障碍服务」，重启时自动弹电源菜单+大字引导；
6. **（推荐）激活流量自动管控**（v1.1）：
   - 电脑执行：`adb shell pm grant com.safphere.launcher android.permission.WRITE_SECURE_SETTINGS`
   - 之后「剩余流量不足自动关闭移动数据」即可静默生效（Android 14+ 上 Device Owner 方式已失效，用此命令）；
   - 不执行此命令：检测/解析/提醒仍然工作，只是"自动关数据"退化为通知提示；
7. **（可选）AI 问答**：子女模式 → AI 设置 → 填 API Key（默认智谱 GLM `glm-4.5-flash` 免费模型，也支持任何 OpenAI 兼容接口）；不填 Key 仍可问时间/日期/电量、语音说「打电话给儿子」；
8. **（可选）白名单/选择性接管**：子女模式 →「选择老人可以看到的应用」勾选白名单；打开「显示全部应用」开关则不屏蔽任何应用（抽屉显示全部）。

## 桌面使用说明（老人视角）

**设计原则：正常时安静，异常时大声。** 呼叫页没有任何常驻状态信息；只有当出现异常（电话卡无卡 / 流量不足已关 / 电量≤20%）时，顶部才会出现一条红色提醒，点击即处理。

**三页内容**（左右滑动，或直接点底部按钮，不依赖滑动）：
1. **📞 快捷呼叫**：1 寸照片式联系人卡（照片+姓名），**点照片直接呼出**（语音播报"正在呼叫X"）；**长按卡片**=发短信/编辑。大小子女可调：每行 1/2/3 个（默认每行 2 个），联系人可添加任意多个
2. **🌤️ 生活**：左边实时大时钟｜右边当日日期+农历；中间天气大卡（三天预报，点卡片语音读天气）；**下方本月日历**（今天红圈高亮，◀▶翻月，**点任意日期弹大字详情：公历+星期+农历**）
3. **📱 常用应用**：抖音/微信等白名单应用大图标（图标大小每行 2/3/4 个可调），点开即用并语音播报；**长按底部「常用应用」按钮 = 打开短信收件箱**

**底部导航栏**（白色圆角卡片，四个彩色圆图标）：🎤 语音问答（打开AI问答页）｜ 🌤️ 生活（跳到生活页）｜ 📱 常用应用（跳到应用页；**长按 = 打开短信收件箱**）｜ 🔁 一键重启（红字警示，确认后重启）——三页通用，不用会滑动也能到达任何页面。

**流量查询入口说明**：按需求已去掉生活页/桌面的流量显示与手动查询行；每天 09:00 仍会自动发短信查询并执行"不足关闭/月结恢复"，剩余不足时会以顶部红色提醒条 + 通知形式出现；子女可随时在「子女模式 → 流量智能管理 → 立即查询流量」手动查询。

**通话中**：桌面顶部自动出现红色大条「📞 通话中 · 点这里挂断电话」，点一下即挂断（首次点击需允许"管理通话"权限；拒绝则跳拨号盘的"返回通话"）。

**子女设置入口（隐藏）**：3 秒内连点左下角「乐龄桌面」水印 5 次（第 2 次起有提示），进入还需 PIN 密码（默认 1234）——防止老人误触。天气城市在子女模式「生活页」分组中修改。

## 4. 功能验证结果（Android 16 x86_64 模拟器实测）

| 功能 | 结果 | 证据 |
|------|------|------|
| 桌面接管+图标屏蔽 | ✅ | 设默认桌面 Success；抽屉仅显示白名单 9 个应用（截图 22） |
| 联系人卡片拨号 | ✅ | 导入「儿子」→ 点卡片 → InCallUI 实际呼出（截图 09/10/11） |
| SIM 无卡警报 | ✅ | 全屏大字警报 + 立即重启/知道了（截图 12/13） |
| 每日 SIM 检测排期 | ✅ | dumpsys alarm：次日 09:00+30min 窗口（重启后自动重排） |
| 一键重启（Device Owner） | ✅ | 确认后 20:20:05 设备离线 → 20:20:18 重启完成，数据完好（截图 20/21） |
| 一键重启（无权限降级） | ✅ | 大字引导长按电源键（截图 14） |
| 子女模式 PIN | ✅ | 默认 1234 可改；全部配置持久化（截图 17/18/36） |
| AI 离线技能 | ✅ | 问时间 →「现在是12点22分」（截图 24） |
| AI 大模型链路 | ✅ | mock OpenAI 服务端到端：请求带 Bearer/模型名，回答大字显示+朗读（截图 40/41） |
| 语音识别（按住说话） | ✅ | 识别器启动，无音频时友好提示（截图 42/43） |
| 流量查询短信（v1.1） | ✅ | 点桌面流量标签发送查询短信至 10086，20分钟节流（截图 45） |
| 短信解析剩余流量（v1.1） | ✅ | 注入 5 种运营商文案均正确解析（86.5MB / 2.3GB 等，含 GB→MB 换算） |
| 低流量自动关数据（v1.1） | ✅ | 66.6MB < 100MB → `mobile_data` 1→0，标签变红「流量不足·数据已关」（截图 49） |
| 月结自动恢复（v1.1） | ✅ | 强制月结检查 → `mobile_data` 0→1，`flow_last_reset_ym` 防重复，自动重新查询（截图 51） |
| 高流量自动恢复（v1.1） | ✅ | 注入 2.3GB → 数据自动重开，标签变绿「流量 2.3GB」（截图 52/53） |
| 子女模式流量配置（v1.1） | ✅ | 运营商/查询短信/阈值/月结日配置渲染并可持久化（截图 54） |
| 选择性接管（v1.1） | ✅ | 「显示全部应用」开关：抽屉 9 个 ↔ 16 个应用切换（截图 55） |
| 两页桌面（v1.2） | ✅ | 第1页快捷呼叫/第2页常用应用；按钮+滑动翻页、页指示点、页标题（截图 62/63） |
| 应用页启动+播报（v1.2） | ✅ | 点 Camera 图标 → 相机前台启动，语音播报「正在打开…」（截图 66） |
| 隐藏入口（v1.2） | ✅ | 品牌连点5次 → PIN 门；桌面无「子女设置」字样（截图 67） |
| 生活页·时钟农历（v1.3） | ✅ | 23:15:24 秒级跳动；农历七月廿五（截图 86/87） |
| 生活页·在线天气（v1.3） | ✅ | 北京实时 23°C 阴·20~30° + 明/后天预报 + 贴心提示；点击语音播报；30分钟节流+离线缓存（截图 86） |
| 生活页·月历（v1.3） | ✅ | 42格完整、今天红圈高亮（像素级确认）、◀▶翻月（截图 87） |
| 真呼出（v1.4） | ✅ | 点头像 ACTION_CALL → InCallActivity 通话计时 00:05 实际接通（截图 91/92）；无权限时当场申请并即时拨出 |
| 顶部速览+四按钮导航（v1.4） | ✅ | 天气速览☁️25°阴点→生活页；电量≤20%变红；四按钮任意页直达（截图 90/93/94） |
| 生活页去重设计（v1.4） | ✅ | 移除重复时钟；日期头+放大天气卡（三天竖排预报）+日历42格（截图 94） |
| 联系人卡双操作（v1.5） | ✅ | 头像区/呼叫钮=真呼出、短信钮=发短信，分区明确（截图 95/96） |
| 呼出确认（v1.5） | ✅ | 大字确认框「呼叫 儿子 13800138000」→确认→真实呼出（截图 97） |
| 真实发短信（v1.5） | ✅ | 常用短语→发送→系统 sms/sent 实际写入「我很好，勿念」→13800138000，自动返回（截图 98/99） |
| 查看短信（v1.5） | ✅ | 长按「常用应用」→ Messages 会话列表显示儿子/10086 会话（截图 104） |
| 通话中挂断（v1.6） | ✅ | 拨号→桌面 2 秒内出现红色挂断条→点一下真挂断（TelecomManager.endCall），通话栈清空（截图 105→108） |
| 卡片视觉修复（v1.6） | ✅ | 两列大卡、头像正圆（像素验证四角白/中心彩）、呼叫/短信按钮归位卡底（截图 105/106） |
| 呼叫页降噪（v1.7） | ✅ | 顶部信息堆+页标题全移除，联系人从 y=53 直接开始（截图 109/110） |
| 信息归位生活页（v1.7） | ✅ | 大时钟+日期+农历+「流量·电量」状态行+天气卡+月历一屏完整（截图 111） |
| 异常提醒条（v1.7） | ✅ | 仅 SIM无卡/流量异常/电量低 时出现红色条，正常时隐藏，点击按优先级处理 |
| 照片式联系人+直接呼叫（v1.8） | ✅ | 点照片立即 InCallActivity 呼出；长按菜单发短信/编辑（截图 112/115） |
| 联系人大小可调（v1.8） | ✅ | 每行1/2/3单选→prefs 持久化→桌面即时变列（截图 117→119） |
| 左时间右日历+日期详情（v1.8） | ✅ | 生活页左时钟/日期/农历，右迷你月历；点15号→「2026年9月15日 星期二 农历八月初五」（截图 113/114） |
| 应用图标大小可调（v1.8） | ✅ | 每行2/3/4可调（与联系人同一机制，prefs 持久化） |
| 全功能回归 v1.9.5 | ✅ | 添加第二联系人（界面）、照片直呼、挂断条、长按菜单、真实发短信落库、短信收件箱、SIM警报、低流量关数据/恢复、AI离线技能+mock大模型链路、四按钮交互、真实重启（v1.6）——全部通过 |
| 签名release包 | ✅ | 乐龄桌面-v1.9.5-release.apk，apksigner 验证证书 CN=Safphere（SHA-256 22b59975…），R8 优化，2.1MB |

## 5. 源码结构

```
app/src/main/java/com/leling/launcher/
├── SafphereApp.kt                 # 入口：白名单初始化、TTS、SIM广播动态注册、每日闹钟
├── home/HomeActivity.kt         # 主桌面（Launcher）：时钟/状态/联系人网格/三大按钮
├── contacts/ContactEditActivity.kt  # 联系人：导入/头像(相册·预设)/长号/短号
├── sim/SimMonitor.kt            # SIM检测核心（ABSENT判定/警报/排期/节流）
├── sim/{SimAlertActivity, DailyCheckReceiver, BootCompletedReceiver}.kt
├── reboot/RebootManager.kt      # 一键重启三级降级：DO静默 → 电源菜单 → 图文引导
├── flow/FlowMonitor.kt          # 流量智能管理：查询短信/解析/关数据/月结恢复
├── flow/{SmsReceiver, FlowInboxCheckReceiver}.kt  # 运营商短信接收（动态+静态）/收件箱兜底
├── admin/ElderDeviceAdminReceiver.kt  # 设备管理员（Device Owner 目标组件）
├── a11y/ElderAccessibilityService.kt  # 无障碍（GLOBAL_ACTION_POWER_DIALOG）
├── ai/{AiChatActivity, LlmClient, LocalSkills}.kt  # 问答UI/OpenAI兼容客户端/离线技能
├── settings/{SettingsActivity, AppPickerActivity}.kt  # 子女模式/PIN/白名单
├── setup/SetupWizardActivity.kt # 首启向导
├── data/{Prefs, Contact}.kt     # SharedPreferences+JSON 存储
├── tts/Speaker.kt               # 中文TTS播报
└── util/{AvatarUtils, Dialogs}.kt
```

## 6. 已知限制与生产建议

1. **方言识别**：系统 `SpeechRecognizer` 识别普通话（国行小米可能无引擎，App 已降级为打字输入）；粤语/四川话等 23 种方言需接讯飞听写 SDK（在线 500 次/天免费），替换点集中在 `AiChatActivity.startListening()`，UI 不变；
2. **Device Owner**：需手机无任何账号时 adb/二维码配置，这是系统限制（所有 MDM 软件同规则）；
3. **自动关数据的兼容性**：`DevicePolicyManager.setMobileDataEnabled` 已从 Android 14+ 框架移除，新系统请使用 `WRITE_SECURE_SETTINGS` 授权路径（见上文第 6 步）；两条路径均失败时退化为通知提醒；
4. **流量短信解析**：运营商文案各省不同，解析失败不动作；真机首月建议子女观察一次查询结果；
5. **MIUI 省电**：真机建议在设置中给本 App「自启动」+ 省电策略「无限制」，否则后台 SIM 广播/闹钟/短信接收可能被系统限制；
6. release 构建：当前交付 debug APK；正式发布请配置签名执行 `gradlew assembleRelease`；
7. 商店上架需向应用市场说明 QUERY_ALL_PACKAGES（桌面枚举应用）与默认桌面/无障碍/短信权限用途。

## 7. 默认配置

- SIM 检测时间：每天 09:00（可改）｜警报冷却：2 小时
- 流量查询：随每日 09:00 检查自动发送｜默认移动 10086 发 CXLL｜阈值 100MB｜月结日 1 号
- 天气城市：北京（子女模式可改，改后自动重新定位）｜每 30 分钟更新｜离线显示上次结果
- 子女模式 PIN：`1234`（请务必修改）
- AI：智谱 GLM `glm-4.5-flash`（免费档），兼容任何 OpenAI 格式 baseUrl/model/key
- 常用应用默认白名单：电话/短信/相机/相册/设置/时钟/日历/浏览器/微信（存在则显示）｜「显示全部应用」默认关

## 8. v1.1 流量功能真机注意事项

1. 运营商回复短信格式各省不同：App 只在解析成功时才动作，解析失败保持「未查询」不会误关数据；子女可在子女模式改查询号码/内容（如电信 10001 发 108）；
2. 自动关数据需 `WRITE_SECURE_SETTINGS`（adb 授予）或旧系统上的 Device Owner；两者都无时退化为高优先级通知提醒；
3. 查询短信为运营商免费短信；发送频控 20 分钟一次；
4. 双卡设备仅管控默认数据卡（`Settings.Global "mobile_data"`）；
5. 数据被关闭期间：打电话、WiFi 上网、收发短信均不受影响。

## 9. 安全与性能审计报告（v2.0.1）

### 安全
| 检查项 | 状态 | 说明 |
|--------|------|------|
| 组件导出面 | ✅ 已收紧 | 仅导出系统必需组件：桌面Home、开机/短信接收器（受 BROADCAST_SMS 系统权限保护）、无障碍/设备管理员（受绑定权限保护）、调试广播（release 中直接忽略）；SIM警报页已停止导出，第三方应用无法伪造警报（debug_force 仅 debug 构建生效） |
| 敏感日志 | ✅ 已脱敏 | release 不再记录短信内容、联系人姓名/号码、卡号等；仅保留 W 级失败日志（无敏感参数） |
| 云备份 | ✅ 已关闭 | allowBackup=false，联系人数据不出设备 |
| 网络通信 | ✅ | 天气/大模型为用户主动配置的服务；默认 HTTPS 端点；支持自定义局域网 HTTP（明文许可已在配置中声明） |
| 权限最小化 | ✅ | 全部权限与功能一一对应；两条高危能力（静默重启/自动关数据）需子女 adb 显式授予 |
| 隐私数据存储 | ✅ | 仅本地 SharedPreferences（联系人/设置），无第三方 SDK、无统计上报、无广告 |
| SMS 权限防护 | ✅ | 短信接收器仅响应系统广播（受保护），查询短信发往子女配置的运营商号码 |

### 性能（Android 16 模拟器实测）
| 指标 | 数值 | 说明 |
|------|------|------|
| 页面热启动 | 121ms | am start -W TotalTime |
| 内存占用（生活页） | Native 19.6MB + Java 6.4MB（已用/上限30MB） | 无 Graphics 泄漏，View 141 个/单 Activity |
| 位图缓存 | 堆的 1/8 自动配额 | 联系人头像后台解码 + LruCache，滚动零主线程解码 |
| 应用列表加载 | 后台线程 | 主线程零枚举（修复前每次进应用页卡顿数百 ms～秒级） |
| 后台唤醒 | 通话状态 2s 轮询仅在桌面/通话后运行 | 其余时间静默 |
| 崩溃 | FATAL EXCEPTION = 0 | 全程回归 logcat 校验 |

## 10. 对外集成快速参考（v2.1 新增）

**推送警报**（集成方需声明 signature 级权限 `com.safphere.launcher.permission.PUSH_ALERT`，即与本应用同签名或平台签名）：

```kotlin
sendBroadcast(Intent("com.safphere.launcher.action.PUSH_ALERT")
    .setPackage("com.safphere.launcher")
    .putExtra("type", "EARTHQUAKE")            // 来源类型
    .putExtra("title", "地震预警")              // 必填
    .putExtra("message", "预估烈度5.5")         // 正文
    .putExtra("icon", "🌊")                     // 可选
    .putExtra("speak", "地震了请就近躲避")       // 可选，TTS
    .putExtra("level", "HIGH")                  // HIGH=全屏 NORMAL=通知
    .putExtra("action", "NONE")                 // 或 REBOOT（SIM类场景）
    .putExtra("source", "地震预警App"))
```

**查询状态**：`content://com.safphere.launcher.status/status`
→ sim_absent / flow_remaining_mb / flow_data_off / battery_pct / updated_at

完整契约、字段校验规则、扩展新警报源的三步做法，见 `docs/开发指南.md`。
测试工具：`tools/AlertTestSender/AlertTestSender.apk`（同签名测试发送端，安装后打开即推送示例地震警报）。
